  /* Returns an empty array if row is null, otherwise returns the rows */
  function returnRows(rows) {
    if (!rows) {
      return [];
    }
    return rows;
  }
  
  module.exports = {
    returnRows
  }